#include "main"
/**
 * _abs - Print tje absolute value of an integer
 *
 * @i: input number as integer
 *
 * Return: absolute value
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (1 * -1);
	}

}
