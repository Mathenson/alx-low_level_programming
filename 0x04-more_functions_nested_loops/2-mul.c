#include "main.h"

/**
 * mul - return the multiplication of two integers
 * @a: first integer
 * @b: second integer
 * Return: Return the mulplication of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
